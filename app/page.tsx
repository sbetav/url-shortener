import { FC } from "react";

interface PageProps {}

const Page: FC<PageProps> = ({}) => {
  return (
    <main className="flex h-dvh w-full items-center justify-center">Page</main>
  );
};

export default Page;
